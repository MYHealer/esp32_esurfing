menu "ESurfingClient 配置"

    config ESURF_WIFI_SSID
        string "WiFi SSID (校园网名称)"
        default "ChinaNet"
        help
            输入要连接的天翼校园网 WiFi SSID。

    config ESURF_WIFI_PASSWORD
        string "WiFi 密码"
        default ""
        help
            输入校园网 WiFi 密码。
            如为开放网络 (无密码), 留空即可。

    config ESURFING_SESSION_TIMEOUT_MIN
        int "Session 主动续期时间 (分钟)"
        default 20
        range 5 120
        help
            校园网单次认证的持续时间。ESP32 会在此时间到期前
            主动登出并重新认证，避免被动掉线。
            建议设为实际 session 超时的 80% 左右。
            默认 20 分钟，适用于 30 分钟超时的校园网。

endmenu
